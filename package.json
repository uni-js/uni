{
  "name": "@uni.js/monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "jest": "^27.4.5",
    "lerna": "^4.0.0",
    "ts-jest": "^27.1.2",
    "wsrun": "^5.2.4"
  },
  "scripts": {
    "build": "yarn wsrun -r -s run build",
    "test": "jest"
  }
}
