{
  "name": "@uni.js/monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "jest": "^27.4.5",
    "lerna": "^4.0.0",
    "ts-jest": "^27.1.2"
  },
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "jest"
  }
}
